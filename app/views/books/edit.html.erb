<h1>Edit Book</h1>
<%= form_with model: @book ,method: :patch do |f| %>

  <p>title</p>
  <%= f.text_field :title %>
  <p>opinion</p>
  <%= f.text_area :body %>

  <%= f.submit "Update Book", class: "btn btn-success"%>
<% end %>